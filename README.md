
### 1. create a .env file

# .env file
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
# Alternatively, a single connection string URL can be used
# DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

- Important: Add .env to your .gitignore file to prevent accidentally committing sensitive information to version control. 

### 2. Run the part1_data_collection_database.ipynb file 
